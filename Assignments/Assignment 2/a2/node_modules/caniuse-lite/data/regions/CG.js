    Editor& operator=(const Editor& source) {
        if (this != &source) { // Check for self-assignment
            // Clean up existing resources
            delete[] m_Title;
            delete[] m_Newspapers;

            // Allocate new memory and copy the elements
            m_Title = new char[MAX];
            strcpy(m_Title, source.m_Title);
            m_NumberOfNewspapers = source.m_NumberOfNewspapers;
            m_Newspapers = new Newspapers*[m_NumberOfNewspapers];
            for (int i = 0; i < m_NumberOfNewspapers; ++i) {
                m_Newspapers[i] = new Newspapers(*source.m_Newspapers[i]);
            }
        }
        return *this; // Return a reference to the current object
    }
